# -*- coding: utf-8 -*-
"""final_project_car_evaluation_dataset.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1HHDqvSqASfqof8KqxFGa138zv5PaPJaA
"""

#### TODO1: Data Description

"""### Dataset Information

#### Introductory Paper
**Knowledge acquisition and explanation for multi-attribute decision making**  
By M. Bohanec, V. Rajkovič. 1988  
Published in 8th Intl Workshop on Expert Systems and their Applications, Avignon, France

#### Variables Table
| Variable Name | Role    | Type        | Description                           | Units | Missing Values |
|---------------|---------|-------------|---------------------------------------|-------|----------------|
| buying        | Feature | Categorical | buying price                          |       | no             |
| maint         | Feature | Categorical | price of the maintenance              |       | no             |
| doors         | Feature | Categorical | number of doors                       |       | no             |
| persons       | Feature | Categorical | capacity in terms of persons to carry |       | no             |
| lug_boot      | Feature | Categorical | the size of luggage boot              |       | no             |
| safety        | Feature | Categorical | estimated safety of the car           |       | no             |
| class         | Target  | Categorical | evaluation level (unacceptable, acceptable, good, very good) |       | no             |


#### Additional Variable Information
- **buying**: vhigh, high, med, low.
- **maint**: vhigh, high, med, low.
- **doors**: 2, 3, 4, 5more.
- **persons**: 2, 4, more.
- **lug_boot**: small, med, big.
- **safety**: low, med, high.

#### Class Labels
- unacc, acc, good, vgood

#### Source
- [Car Evaluation](https://archive.ics.uci.edu/dataset/19/car+evaluation) on UCI Machine Learning Repository
"""

# Commented out IPython magic to ensure Python compatibility.
# import libraries for data processing
import pandas as pd
import numpy as np

# import libraries for data visualization
import matplotlib.pyplot as plt
import seaborn as sns
# %matplotlib inline

# ignore warnings
import warnings
warnings.filterwarnings('ignore')

from google.colab import drive
drive.mount('/content/drive')

# Load the 'adult_data.csv' dataset into a Pandas DataFrame
# data = pd.read_csv('train.csv')
data = pd.read_csv('/content/drive/MyDrive/Collab(draft)/car.data', header = None)
# Display the number of rows and columns in the dataset
print(data.shape)

#### TODO2: Data size and dimension

"""### Data size and dimension

This dataset consists of **1727 rows** and **7 columns**. Each row represents a unique car evaluation, and each column corresponds to a specific attribute or feature of the car. The columns include:

1. **buying**: Buying price of the car (Categorical)
2. **maint**: Maintenance price of the car (Categorical)
3. **doors**: Number of doors in the car (Categorical)
4. **persons**: Capacity in terms of persons to carry (Categorical)
5. **lug_boot**: Size of the luggage boot (Categorical)
6. **safety**: Estimated safety of the car (Categorical)
7. **class**: Evaluation level of the car (Target variable, Categorical)



"""

# Display the first 5 rows of the dataset
# This is useful to understand the data's structure, column names, and sample values
data.head()

data.columns = ['buying', 'maint', 'doors', 'persons', 'lug_boot', 'safety', 'class']

data.head()

data.info()

#### TODO3: Data Information

"""### Data Information

**Missing Data**: There are no missing values in any of the columns, as indicated by the non-null count matching the total number of entries (1727) for each column.

**Column Names and Data Types**:
- The column names (`0`, `1`, `2`, `3`, `4`, `5`, `6`) has been changed to (`buying`, `maint`, `doors`, `persons`, `lug_boot`, `safety`, `class`) for better understanding and consistency.
- All columns are of type `object`, which is appropriate for **buying**, **maint**, **lug_boot**, **safety**, and **class**. However, it is not appropriate for **doors** and **persons**; these two data should be in `int` type.

**Data Types**: The categorical variables are represented as `object` type.

**Memory Usage**: The dataset uses 94.6 KB of memory, which is manageable for most analyses.

**Overall**: The dataset is clean with no missing values, and the data types are relatively appropriate for the categorical nature of the variables. However, the columns should be renamed for clarity would be beneficial and the datatypes of `doors` and `persons` should be changed to facilitate subsequent analysis.
"""

# Identify missing data
missing_data = data.isnull().sum()

# Visualize missing data
plt.figure(figsize=(6, 4))
sns.heatmap(data.isnull(), cmap='viridis', cbar=False)
plt.title('Missing Data Visualization')
plt.show()

# Print missing data counts
print("Missing Data Counts:\n", missing_data)

#### TODO4: Missing values

"""# Missing Data Analysis

The analysis of missing data in the dataset reveals that there are no missing values across all columns. This is confirmed by the `missing_data` output, which shows zero missing values for each column.


The heatmap visualization further supports this finding, as it does not display any gaps or missing data points. This indicates that the dataset is complete and does not require any imputation or handling of missing values.

"""

# Identify duplicate data
duplicate_rows = data[data.duplicated()]

# Display duplicate rows if they exist
if not duplicate_rows.empty:
    print("\nDuplicated Rows:")
    print(duplicate_rows)
else:
    print("\nNo duplicated rows found.")

# Print the count of duplicate rows
print(f"\nNumber of Duplicated Rows: {len(duplicate_rows)}")

#### TODO5: Duplicated Values

"""# Duplicate Data Analysis

The analysis of duplicate data in the dataset indicates that there are no duplicate rows. This is confirmed by the output, which shows zero duplicated rows.


The code snippet used to identify duplicate rows verifies this finding by checking for any duplicated entries and printing the count. Since no duplicates were found, the dataset is free from any redundant data entries, ensuring its integrity and reliability.

"""

# Dropping duplicates and resetting the index
data = data.drop_duplicates().reset_index(drop=True)
print(data.shape)

# Generating summary statistics of the numerical columns
data.describe()

data['doors'] = data['doors'].replace('5more', '5')
data['doors'] = data['doors'].astype(int)

data['persons'] = data['persons'].replace('more', '5')
data['persons'] = data['persons'].astype(int)

# Generating summary statistics of the numerical columns
data.describe()

#### TODO6: General Evaluation for Numeric Values

"""### General Evaluation of Numeric Values

 **Numeric Columns**:
   - The dataset includes the following numeric columns: `doors`, `persons`.
   
### Analysis
- **Unique Values**: At the begining, the unique values in these two columns `doors` and `persons` is in `object` type. Although it still shows the correct number values, these can be detrimental to the following analysis steps. Therefore, the datatype were changed into `int` type.
- **Central Tendency**: The **mean** and **median** values for both `doors` (at 3.5) and `persons` (at 3.67) are close to each other, indicating a relatively symmetric distribution for these columns.
- **Dispersion**: The **standard deviation** values indicate the spread of the data around the mean. For `doors`, the standard deviation is 1.118, and for `persons`, it is 1.248. This suggests that the number of doors and persons varies moderately around their respective means.
- **Range**: The **range** (difference between the minimum and maximum values) for both columns is 3, indicating a moderate spread of values.
- **Percentiles**: The **percentiles** provide insights into the distribution of the data: For `doors`, 25% of the data has 2.75 or fewer doors, 50% has 3.50 or fewer, and 75% has 4.25 or fewer; and for `persons`, 25% of the data can accommodate 2 or fewer persons, 50% can accommodate 4 or fewer, and 75% can accommodate 5 or fewer.
- **Outliers**: The **minimum** and **maximum** values for both columns are within a reasonable range, suggesting that there are no extreme outliers.
  
### Overall
The numeric columns `doors` and `persons` in the dataset show a consistent and normal distribution with no significant abnormalities. There is only a brief notice about changing the data type for unique values in order to ensure that the data is appropriately organised and suited for both present and future analysis.
"""

# Select numeric data
numeric_data = data.select_dtypes(include='number').columns
print("numeric_data", numeric_data)
# Select categorical data
categorical_data = data.select_dtypes(exclude='number').columns
print("categorical_data", categorical_data)

for col in categorical_data:
    unique_values = data[col].unique()

    # Print unique values for the current column
    print(f"\nUnique Values in Column '{col}':")
    print(unique_values)

    # Optionally, print the value counts if needed
    # print(data[col].value_counts())

#### TODO7: General Evaluation for Categorical Values

"""
### General Evaluation of Categorical Values

1. **Categorical Columns**:
   - The dataset includes the following categorical columns: `buying`, `maint`, `lug_boot`, `safety`, and `class`.

2. **Unique Values**:
   - **buying**: ['vhigh', 'high', 'med', 'low']
   - **maint**: ['vhigh', 'high', 'med', 'low']
   - **lug_boot**: ['small', 'med', 'big']
   - **safety**: ['low', 'med', 'high']
   - **class**: ['unacc', 'acc', 'vgood', 'good']

### Analysis

- **Closeness of Data**:
  - The unique values in each categorical column are well-defined and distinct. This indicates that the categories are clearly separated and there is no overlap between them. Additionally, each unique values all have enough data.

- **Abnormalities**:
  - There are no apparent abnormalities in the unique values of the categorical columns. Each column has a reasonable number of unique values that are expected for the type of data being analyzed.

- **Outliers**:
  - Based on the unique values provided, there are no immediate outliers.

### Overall
The data of categorical values ​​does not look unusual. Each unique value has complete data and no outliers. The data distribution of values ​​will be shown more clearly in the next steps.




"""

for col in numeric_data:
    fig, axes = plt.subplots(2, 1, figsize=(10, 5))  # 2 rows, 1 column

    # Distribution Plot (Top Subplot)
    sns.histplot(data=data, x=col, kde=True, ax=axes[0])
    axes[0].set_title(f"Distribution of '{col}'")
    axes[0].set_xlabel(col)
    axes[0].set_ylabel("Density")

    # Box Plot (Bottom Subplot)
    sns.boxplot(data=data, x=col, ax=axes[1])
    axes[1].set_title(f"Boxplot of '{col}'")
    axes[1].set_xlabel(col)

    # Adjust layout and display
    plt.tight_layout()
    plt.show()

#### TODO8: Univariate Analysis for Numeric Values

""" ### Univariate Analysis for Numeric Values

1. **Number of Columns**:
   - There are **2 numeric columns** in the dataset: `doors` and `persons`.

2. **Shapes Drawn for Each Column**:
   - For each numeric column, two types of plots are drawn:
     1. **Distribution Plot (Histogram with KDE)**: This plot shows the distribution of the data along with a Kernel Density Estimate (KDE) to visualize the probability density function.
     2. **Box Plot**: This plot provides a summary of the data distribution, highlighting the median, quartiles, and potential outliers.

3. **Analysis of Distribution and Histogram Shapes**:
   - **`doors`**:
     - **Data values**: [`2.0`, `3.0`, `4.0`, `5.0`]
     - **Distribution Plot**:  The histogram shows the frequency distribution of the number of doors in the dataset. The x-axis represents the number of doors, ranging from 2 to 5, and the y-axis represents the density. The distribution appears to be relatively uniform, with each category (2, 3, 4, and 5 doors) having a similar frequency. This suggests that the dataset contains a balanced number of vehicles with different door counts. The density curve overlaid on the histogram provides a smooth estimate of the distribution, confirming the uniformity.

     - **Box Plot**: The median value (the line inside the box) is at 3.5, indicating that half of the vehicles have a number of doors less than the value 3.5. The box represents the IQR, which is the range between the 25th percentile (Q1) and the 75th percentile (Q3). For the `doors` variable, Q1 is at 2.75 and Q3 is at 4.25. The whiskers extend to the minimum and maximum values within 1.5 times the IQR from the quartiles. In this case, the whiskers extend from 2 to 5, indicating that these are the minimum and maximum values without any outliers. There are no outliers in the `doors` variable, as all data points fall within the whiskers.

     - **Overall**: The values of `doors` are distributed uniformly and  according to the histogram and the box plot does not display any outliers. Therefore, there are no irregularities in the graph.

   - **`persons`**:
     - **Data values**: [`2.0`, `4.0`, `5.0`]
     - **Distribution Plot**: The histogram shows the frequency distribution of the number of persons that the car can carry in the dataset. The x-axis represents the number of persons, ranging from 2 to 5, and the y-axis represents the density. As opposed to the door's data set, the distribution appears seem to be not uniform, there is not data in the category 3. This causes the data displayed on the bar chart to be spaced far apart. The KDE line shows a data shortage at position value 3.0.

     - **Box Plot**: The median value (the line inside the box) is at 4.0, indicating that more than half of the vehicles can accommodate 4.0 or fewer persons. The box represents the IQR, which is the range between the 25th percentile (Q1) and the 75th percentile (Q3). For the `doors` variable, Q1 is at 2.75 and Q3 is at 4.25. The whiskers extend to the minimum and maximum values within 1.5 times the IQR from the quartiles. In this case, the whiskers extend from 2 to 5 (3.0 times the IQR from the quartiles), indicating that these are the minimum and maximum values without any outliers. There are no outliers in the `persons` variable, as all data points fall within the whiskers.
     - **Overall**: The values of `persons` are not distributed uniformly because of the shortage of data value as shown in the distribution plot and the boxplot. However, similar to the situation `doors`, there are no outliers in the data.



"""

for col in categorical_data:
    plt.figure(figsize=(8, 3))  # Adjust figsize as needed

    # Calculate value counts
    value_counts = data[col].value_counts()

    # Create the horizontal bar plot
    ax = sns.barplot(x=value_counts.values, y=value_counts.index)

    # Enhance plot appearance
    plt.ylabel(col)
    plt.xlabel("Count")
    # plt.xticks(rotation=45)  # No need for rotation with horizontal bars
    plt.grid(axis='x', alpha=0.75)  # Add horizontal grid

    # Add count annotations
    for index, value in enumerate(value_counts.values):
        ax.text(value, index, f'{value}', va='center')

    # Display the plot
    plt.tight_layout()
    plt.show()

#### TODO9: Univariate Analysis for Categorical Values

"""### Univariate Analysis for Categorical Values

1. **Number of Columns**:
   - There are **5 categorical values** in the dataset: `buying`, `maint`, `lug_boot`, `safety`, `class`.

2. **Shapes Drawn for Each Column**:
   - For each categorical column, there is a **Distribution Plot** are drawn, which shows the distribution of the data.
     
3. **Analysis of Distribution and Histogram Shapes**:
   - **`buying`, `maint`**:
     - **Distribution Plot**: There are 4 columns in the histogram for `buying` and `maint` shows the frequency of unique values (`vhigh`, `high`, `med`, `low`).
     -  In `buying` and `maint`, the values ​​are distributed evenly (the frequent of each unique values is `432`). There are no abnormalities in the graphs.
   - **`lug_boot`**:
     - **Distribution Plot**: There are 3 columns in the histogram for `lug_boot` shows the frequency of unique values (`small`, `med`, `big`).
     -  In `lug_boot`, the values ​​are distributed evenly (the frequent of each unique values is `576`). There are no abnormalities in the graphs.
   - **`safety`**:
     - **Distribution Plot**: There are 3 columns in the histogram for `safety` shows the frequency of unique values (`low`, `med`, `high`).
     -  In `safety`, the values ​​are distributed evenly (the frequent of each unique values is `576`). There are no abnormalities in the graphs.
   - **`class`**:
     - **Distribution Plot**: There are 4 columns in the histogram for `class` shows the frequency of unique values (`unacc`, `acc`, `good`, `vgood`).
     -  In `class`, the values ​​are distributed unevenly (the frequent `unacc` is count for `1210` out of `1728`). This implies that the majority of the cars in the sample are viewed as unsatisfactory.

### Overall
In general, except for the uneven distribution of `class` data, the remaining categorical data in this dataset is evenly distributed. In addition, no abnormalities were detected in the data.

"""

# Calculate the correlation matrix
corr_matrix = data[numeric_data].corr()

# Plot the heatmap
plt.figure(figsize=(8, 6))
sns.heatmap(corr_matrix, cmap='coolwarm', cbar=True, square=True, linewidths=.5)
plt.title('Heatmap of Correlation Matrix')
plt.show()

#### TODO10: Evaluation for Correlated Data

"""# Heatmap of Correlation Matrix

### 1. Purpose of the Heatmap:
The heatmap visualizes the correlation matrix for five numeric columns: `doors` and `person`. The color scale represents the strength and direction of the correlation between each pair of variables, with red indicating strong positive correlations and blue indicating weaker correlations.

### 2. Analysis of Correlation Patterns:
Diagonal Elements:
- The diagonal elements (top-left and bottom-right) are red, indicating a perfect positive correlation (correlation coefficient of 1.0) between each variable and itself. This is expected as any variable is perfectly correlated with itself.

Off-Diagonal Elements:
- The off-diagonal elements (top-right and bottom-left) are blue, indicating a low or no correlation (correlation coefficient close to 0.0) between the variables `doors` and `persons`. This suggests that there is no linear relationship between the number of doors and the number of persons.

Relevance:
- This heatmap is relevant for understanding the relationships between different variables in a dataset. In this case, it shows that the number of doors and the number of persons are not correlated, which could be useful information for further analysis or decision-making.

"""

#### TODO17: Overall Conclusion